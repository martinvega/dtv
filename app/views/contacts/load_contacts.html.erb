<% unless @contact.nil?  %>
  <%= render :partial => 'contact' %>
<% end %>

<%= form_tag load_contacts_contacts_path, :method => 'get' do %>

  <div class="field">
    <%= select_tag :campaign, options_from_collection_for_select(Campaign.all, "id", "date") %>
  </div>

<div id="links">
  <div id="search_contact">
    <%= submit_tag 'Buscar Contacto' %>
  </div>
</div>

  <% end %>

